# Changelog

## 2026-05-22

### Additions and New Features

- **Plugin repair and dev-jump plan closed (M1-M4)**: shipped the full plan, now
  archived at
  [docs/archive/2026-05-plugin_repair_and_devjump.md](archive/2026-05-plugin_repair_and_devjump.md).
  Four milestones landed:
  - **M1 (life jump)**: new `src/dev/` module ships
    [src/dev/dev_jump.ts](../src/dev/dev_jump.ts) (synthetic-player constructor) and
    [src/dev/dev_jump_ui.ts](../src/dev/dev_jump_ui.ts) (fixed-position "Life Jump"
    button in the bottom-right corner). Trigger via `?life=<phase>&age=<n>&team=<id>`
    URL parameter parsed in [src/main.ts](../src/main.ts) before save-load, or via
    the visible button. Phase values: `childhood`, `high_school`, `college`, `nfl`,
    `legacy`. `?dev=` accepted as back-compatible alias. Earlier hidden Alt+Shift+J hotkey design was replaced with the visible
    button so testers can discover it without being mid-flow. Regression coverage in
    [tests/test_dev_jump.ts](../tests/test_dev_jump.ts) (12 tests).
  - **M2 (avatar repair)**: avatar internals were healthy; real defects were
    (1) life-jump used invalid registry keys and (2) `avatar_test.html` imported a
    nonexistent `./dist/avatar.js`. Fixes: re-enabled `randomAvatarConfig` in
    `src/dev/dev_jump.ts`, fixed the harness import via a re-export from
    [src/main.ts](../src/main.ts), and hardened `getPart` / `getSkinColor` /
    `getHairColor` in [src/avatar.ts](../src/avatar.ts) to throw on unknown keys.
    Playwright spec [tests/playwright/test_avatar_render.spec.ts](../tests/playwright/test_avatar_render.spec.ts)
    covers six archetypes structurally.
  - **M3 (plugin audit)**: scout_report panel never rendered because it targeted a
    nonexistent `#panel-content` container; fixed to `#career-content` and wired into
    [src/ui/career_widget.ts](../src/ui/career_widget.ts) for college and NFL phases.
    Per-plugin tests added: [tests/test_plugin_childhood.ts](../tests/test_plugin_childhood.ts)
    (13), [tests/test_high_school_plugin.ts](../tests/test_high_school_plugin.ts) (6),
    [tests/test_plugin_college.ts](../tests/test_plugin_college.ts) (11),
    [tests/test_nfl_plugin.ts](../tests/test_nfl_plugin.ts) (7), and
    [tests/test_scout_report_panel.ts](../tests/test_scout_report_panel.ts) (6).
  - **M4 (lifecycle hooks wiring)**: hooks were registered but never `.fire()`'d in
    production. Built shared utility
    [src/plugins/lifecycle_hooks_runner.ts](../src/plugins/lifecycle_hooks_runner.ts)
    exporting `fireAgeHooks(player, ctx)`, `firePhaseStartHooks(phase, player, ctx)`,
    and `fireCareerEndHooks(trigger, player, ctx)`. Wired into year handlers under
    `src/childhood/`, `src/high_school/`, `src/college/`, `src/nfl_handlers/`, and
    [src/legacy/retirement.ts](../src/legacy/retirement.ts). Extended
    `RetirementContext` to carry a required `careerContext` field so retirement can
    fire the `CareerEndHook`. Regression coverage in
    [tests/test_lifecycle_wiring.ts](../tests/test_lifecycle_wiring.ts) (8 tests) plus
    the per-plugin suites above.

  Total test count grew from 56 baseline to 120 across the four milestones; shared
  factories live in [tests/test_helpers.ts](../tests/test_helpers.ts).

### Behavior or Interface Changes

- **Life Jump: rebrand Dev Jump as a player feature**: reframed "Dev Jump" as "Life Jump",
  a player-facing feature that lets you skip ahead to any phase + age. Updated button text,
  overlay title, and IDs. Docs now describe it as a gameplay feature, not developer-only.
  URL parameter changed from `?dev=` to `?life=`, with `?dev=` still accepted for
  backward compatibility. Module headers updated to reflect player-facing purpose.

- **Scout report panel render target corrected**: panel previously targeted a
  nonexistent `#panel-content` container, so it never rendered even though it was
  registered. Fixed to `#career-content` and wired into
  [src/ui/career_widget.ts](../src/ui/career_widget.ts) so the panel renders when the
  career tab is viewed in college or NFL phases. Render now mounts into a contained
  `#scout-report-section` to avoid colliding with other career panel content.

- **Four-phase strict-mode recovery closed**: Completed four-phase recovery (Phases 1-4) from
  starter-repo sync commit 33642de, which introduced 454 typecheck errors and broken ESLint config.
  Plugin runtime stayed healthy throughout (56/56 node:test PASS).

  Phase 1 (reduce error surface, 454 to 273): scoped `tsconfig.json` from
  `include: ["**/*.ts"]` to `include: ["src/**/*.ts", "tests/**/*.ts"]` and added
  `exclude: ["node_modules", "dist", "_site", "archive", "tools/_out"]`. Mirrored exclude into
  `tsconfig.lint.json`. Added `@eslint/js` and `globals` to `package.json` devDependencies
  (propagated `eslint.config.js` imported them but they were never installed -- lint crashed with
  `Cannot find package '@eslint/js'`). Extended `eslint.config.js` ignores to `archive/**`,
  `tools/**`, `_site/**`, `tools/_out/**`, `tests/**/*.mjs`.

  Phase 2 (tools/sim*.ts triage): kept four `tools/sim*.ts`analysis scripts plus`tools/sim_conf/{aggregators,display}.ts` in place (active user-facing tools, not dead code).
Fixed 65 strict-flag errors via index-access guards and type-only import conversions. Many
non-null assertions (`!`) used with invariant comments because tsx-run analysis scripts have
lower correctness bar than `src/`.

  Phase 3 (strict-mode fixes in src/ and tests/, 273 to 0): Dispatched 5 parallel coder subagents
  by directory bucket: plugins (7 files), phase*logic (93 files), sim_engine (84 files),
  ui_toplevel (40 files), tests (49 files). All batches landed clean. Fix patterns:
  `noUncheckedIndexedAccess` guard with invariant comment; `exactOptionalPropertyTypes` omit fields
  at call site or conditional spread; `noUnusedLocals`/`noUnusedParameters` rename to `_param`
  per `argsIgnorePattern: '^*'`ESLint exemption;`verbatimModuleSyntax`use`import type`swaps.
Tests batch added`/// <reference types="node" />` to each test file to bring Node globals in
  scope under root tsconfig.

  Phase 4 (gate realignment): rewrote three `step_run` lines in `check_codebase.sh` to delegate to
  npm scripts (`npm run lint`, `npm run test:node`, `npm run format:check`) rather than invoking
  tools directly with stale globs. Critical fix: dropped `--max-warnings 0` from lint step.
  Pre-existing `npx eslint --max-warnings 0` contract conflicted with
  [docs/TYPESCRIPT_STYLE.md](TYPESCRIPT_STYLE.md) which says `no-console: warn` is intentionally
  warn-only ("user decision: warn only, do not fail builds"). Inline `node --test 'tests/test_*.mjs'`
  was finding only 1 of 56 tests because tsx-based runner ships .ts test files; switching to
  `npm run test:node` (which uses `node --import tsx --import csv_loader.mjs --test 'tests/test_*.ts'`)
  brought all 56 back into gate. Ran `npm run format:write` once to drain 16 Phase-3 format issues.
  Header comment refreshed to describe npm-delegation model.

  Final gate state: all five npm-script gates now pass. 70 files changed, 880 insertions, 713
  deletions.

### Fixes and Maintenance

- **C5 (NFL veteran retire path): set phase + save + sync only.** Original C5 fix
  routed the veteran Retire action through `showRetirement()`, but importing
  `render/story_log` from `src/nfl_handlers/` tripped `tests/test_dom_imports.ts`
  (no DOM in simulation tree). Reverted to setting `player.phase = 'legacy'`,
  calling `ctx.save()`, and `ctx.syncTabsToPhase('legacy')`. `showRetirement`
  (and `fireCareerEndHooks`) is invoked by `src/main.ts:resumeGame` on next game
  load when it detects `phase === 'legacy'`. Known limitation: in-session
  retirement leaves the player without a forward UI interaction until reload. Real
  fix requires a year-handler->main signaling layer; tracked as a separate residual.

- **Fix C4 careerContext required in RetirementContext**: `src/legacy/retirement.ts`
  had `careerContext?: CareerContext` as optional with a defensive `if (ctx.careerContext)` guard
  around `fireCareerEndHooks()`. This violated "fix the design, not the symptom" - the missing wire
  was hidden rather than exposed. Made `careerContext` required in `RetirementContext` interface,
  removed the guard so hooks always fire, and updated the only caller in `src/main.ts`
  `resumeGame()` to pass `careerContext: careerCtx` (with a precondition error if uninitialized).
  TypeScript strict types and tests now prevent the missing-wire scenario.

- **M3 W3.scout_report: plugin panel audit and fix complete**: Audited `src/plugins/scout_report/` panel-only proof-of-concept plugin. Issue: `scout_report_panel.ts` tried to render to non-existent `#panel-content` container; panel was registered but never invoked. Minimal fix: (1) Changed `scoutReportPanel.render()` to target `#career-content` (the actual career tab content area) instead of non-existent `#panel-content`; (2) Updated `renderScoutReport()` to create a contained `#scout-report-section` within the container to avoid colliding with other career panel content; (3) Integrated scout_report panel into `updateCareerTab()` in `src/ui/career_widget.ts` so it renders when the career tab is viewed in college or nfl phases. Added 6 verification tests in new `tests/test_scout_report_panel.ts` covering: panel registration, phase availability (college/nfl only, not high_school), and render-without-error paths for both college and nfl players. All tests green. `tests/test_plugin_boundaries.ts` still passes (no cross-plugin imports introduced). Per plan scope: no registry changes needed (scout_report is panel-only, no lifecycle hooks).

- **Deleted stale gitignored `_site/` directory**: removed pre-M4 staging tree leftover from
  legacy build model. Directory was noted as cleanup-pending in 2026-05-21 M9 entry.

- **ESLint lint output reconciled to warnings-only**: ran `npm run lint -- --fix` to drain 18
  mechanically-fixable findings (consistent-type-imports, no-unnecessary-type-assertion auto-cases).
  Hand-fixed remaining 129 across top files (tests/test_plugin_host.ts 43, src/nfl.ts 16, others).
  Rule families addressed: `no-floating-promises` via `void` wraps in test setup and timing helpers;
  `no-unused-vars` via `_param` or deletion; `no-unnecessary-type-assertion` by removing redundant
  non-null assertions; `no-useless-escape` in regex character classes; `no-useless-assignment` by
  removing dead writes. Final lint output: 0 errors, 225 warnings (warnings include 148
  explicit-function-return-type and 76 no-console both warn-level by design).

### Decisions and Failures

- **Phase 2 and Phase 3 introduced many non-null assertions in tools/ and tests/**: Each has an
  invariant comment, but long-term correctness story would be guard-based instead. User flagged
  this risk in plan review. Acceptable for tools/tests; flag for revisit if any sim\*.ts script is
  promoted to production use.

- **noUnusedLocals / noUnusedParameters now globally enforced**: Many genuinely unused signature
  parameters were renamed `_arg` rather than deleted because function signature is part of
  interface (e.g., plugin lifecycle hooks). Future plugin authors should either drop these from
  interface contract or keep using underscore pattern.

- **225 ESLint warnings remain**: `explicit-function-return-type` and `no-console` are warn-level
  per [docs/TYPESCRIPT_STYLE.md](TYPESCRIPT_STYLE.md); gate ignores warnings. If warnings should
  be driven to zero, that is separate cleanup pass.

- **Avatar test harness import deferred residual**: `tests/playwright/test_avatar_render.spec.ts`
  is not invoked by `check_codebase.sh`. Snapshot baseline is intentionally not
  committed; the spec uses structural assertions across six archetypes instead of
  pixel diffs. Promote to the gate when a stable headless-render path is wired.

- **`MockGameContext` duplicated across plugin test files**: shared mock context
  helper is duplicated across [tests/test_high_school_plugin.ts](../tests/test_high_school_plugin.ts)
  and [tests/test_nfl_plugin.ts](../tests/test_nfl_plugin.ts). Consolidation into
  [tests/test_helpers.ts](../tests/test_helpers.ts) is deferred; each plugin suite
  works independently today.

- **`tests/playwright/autoplay.mjs` baseline failure is environmental**: not a code
  regression. The failure stems from a stale port-8000 server that does not serve
  `dist/`. Re-verify by starting a `dist/`-serving server (for example
  `python3 -m http.server 8000 --directory dist`) before running autoplay.

### Developer Tests and Notes

- **Gate verification (M4 W4.B close, 2026-05-22)**:
  - PASS: typecheck
  - PASS: typecheck:lint
  - PASS: lint
  - PASS: format:check
  - PASS: test:node (120/120 node:test PASS, up from 56 baseline)
  - `bash check_codebase.sh` reports 5/5 PASS

- **Test suite additions (M1-M4)**:
  | File | Tests | Scope |
  | --- | --- | --- |
  | tests/test_dev_jump.ts | 12 | Synthetic-player constructor, URL-param parsing |
  | tests/test_plugin_childhood.ts | 13 | Childhood handlers + hooks |
  | tests/test_high_school_plugin.ts | 6 | HS handlers + hooks |
  | tests/test_plugin_college.ts | 11 | College handlers + hooks |
  | tests/test_nfl_plugin.ts | 7 | NFL handlers + hooks |
  | tests/test_scout_report_panel.ts | 6 | Panel registration + render |
  | tests/test_lifecycle_wiring.ts | 8 | Cross-handler hook firing |
  | tests/test_helpers.ts | n/a | Shared factories |
  | tests/playwright/test_avatar_render.spec.ts | n/a | Six avatar archetypes |

- **Phase 3E: All 93 strict-mode errors fixed in 17 batch files**: Completed comprehensive fix across all remaining batch files from the approved plan. Applied consistent patterns for noUncheckedIndexedAccess (array[i]! with bounds-check comments), Record access with nullish coalescing (record[key] ?? 0), find() fallback pattern ((arr.find(...) || arr[0])!), unused parameters (\_param convention), and union-type casts (as CoachPersonality). Files fixed: recruiting.ts, ncaa.ts, nfl.ts, milestones.ts, character_creation.ts, kid_years.ts, peewee_years.ts, hs_frosh_soph.ts, hs_season_builder.ts, juco_season_builder.ts, recruiting_events.ts, recruiting_offers.ts, college_entry.ts, college_core.ts, college_senior.ts, college_season_builder.ts, nfl_season_builder.ts. TypeScript strict mode now passes with zero errors in batch files (verified with `npm run typecheck`). No runtime behavior changes; type-only fixes.

- **Gate verification (earlier 2026-05-22 strict-mode close)**:
  - PASS: typecheck
  - PASS: typecheck:lint
  - PASS: lint
  - PASS: format:check
  - PASS: test:node (56/56 node:test PASS at that checkpoint)
  - 70 files changed, 880 insertions, 713 deletions

## 2026-05-21

### Additions and New Features

- **Milestone M-B: test paradigm conversion to node:test (Patch B1/B2/B3/B4)**: replaced the bespoke `tests/run.ts` spawnSync orchestrator with native `node --test` discovery. WP-B1 renamed 3 static-probe boundary checks via `git mv` to match the canonical test prefix:

  ```text
  tests/check_dom_imports.ts         -> tests/test_dom_imports.ts
  tests/check_math_random_budget.ts  -> tests/test_math_random_budget.ts
  tests/check_plugin_boundaries.ts   -> tests/test_plugin_boundaries.ts
  ```

  WP-B2 rewrote all 9 test files to the node:test API. Each former bare-async helper became a top-level `test('module: name', () => { ... })` call; every file now starts with `import { test } from 'node:test'` plus the existing `import assert from 'node:assert/strict'`. Bottom-of-file `main()` calls and per-module `console.log('X: N/N ok')` summaries were deleted; the node:test reporter owns reporting. The 3 boundary-check probes were each wrapped in a single `test('boundary check: ...', ...)` call that throws on violation in place of `process.exit(1)`. The test count expanded from 9 module summaries to 56 named tests:

  ```text
  rng                      7
  player_helpers           4
  handler_registry         3
  simulator                7
  choice_schemas           5 (one per phase)
  plugin_host             27
  dom_imports              1 (single boundary probe)
  math_random_budget       1 (single boundary probe)
  plugin_boundaries        1 (single boundary probe)
  total                   56
  ```

  WP-B3 swapped the `test:node` script value in [package.json](../package.json) from the old `tsx tests/run.ts` shape to:

  ```bash
  NODE_OPTIONS=--disable-warning=DEP0205 node --import tsx --import ./tests/fixtures/csv_loader.mjs --test 'tests/test_*.ts'
  ```

  and deleted `tests/run.ts` via `git rm` (81 lines of orchestrator removed). WP-B4 updated the [docs/USAGE.md](USAGE.md) Tests section with the new invocation plus `--test-name-pattern` and `--watch` examples. Added one rule block to [eslint.config.js](../eslint.config.js) that disables `@typescript-eslint/no-floating-promises` for files under `tests/`; node:test's `test()` returns a Promise the runner itself awaits internally, so without the exemption the 56 top-level `test(...)` calls produced 56 floating-promise false positives. `tests/fixtures/csv_loader.mjs` was not touched (still required for `.csv` text imports under tsx). `tests/playwright/autoplay.mjs` was not touched (separate manual smoke concern). No `src/` source changes required. Verification: `npm run lint` exits 0; `npm run test:node` reports `tests 56 / pass 56 / fail 0` via node:test's spec reporter with every test listed by name; `bash check_codebase.sh` summary `[PASS] typecheck / [PASS] typecheck:lint / [PASS] lint / [PASS] format:check / [PASS] test:node / [PASS] build`, totaling 6 PASS (Playwright is intentionally not part of this gate per the current orchestrator header comment); bundle `dist/main.js` unchanged at 507.4kb (test conversion does not touch bundled source). Residual: node:test's `--test-name-pattern` flag only filters when placed BEFORE the `--test <glob>` positional arg in node's command line. Running `npm run test:node -- --test-name-pattern=X` lands the flag AFTER the glob, where node treats it as a no-op; the filter still works when invoked as the full direct `node --test-name-pattern=X --test ...` form documented in USAGE.md. Plan reference: `~/.claude/plans/dynamic-tinkering-avalanche.md` M-B.

- **Milestone M-A: ESLint rewire with typescript-eslint v8 (Patch A1/A2/A3)**: re-introduced ESLint after the audit-fix-1 deletion, this time wired to actually lint .ts source. Installed `eslint ^10.4.0` and `typescript-eslint ^8.59.4` as devDeps (meta-package = parser + plugin). Created [eslint.config.js](../eslint.config.js) as a flat ESM config: type-aware lint on `**/*.ts` via explicit `project: ['./tsconfig.json', './tsconfig.lint.json']` (projectService default misses tests/ and tools/ because tsconfig.json excludes them); base rules on `**/*.mjs` (no-var, prefer-const, eqeqeq); ignores `dist/**`, `dist-single/**`, `_site/**`, `node_modules/**`, `archive/**`, `tools/sim_conf/**`. Enabled rule set: `typescript-eslint.configs.recommendedTypeChecked` (catches `no-floating-promises`, `no-misused-promises`, `no-unsafe-assignment`, `no-unsafe-return`, `no-base-to-string`, `restrict-template-expressions`, `unbound-method`, `no-unnecessary-type-assertion`, `no-unsafe-enum-comparison`, `require-await`, `no-unused-vars`) plus `@typescript-eslint/no-explicit-any` bumped from warn to error, `@typescript-eslint/consistent-type-imports`: error, and `@typescript-eslint/no-unused-vars` with `argsIgnorePattern: '^_'` / `varsIgnorePattern: '^_'` / `caughtErrorsIgnorePattern: '^_'` so intentionally-unused names can stay via underscore-prefix. Added `lint` npm script (`eslint .`) in [package.json](../package.json) between `format:write` and `test:node`. Added `step_run lint` step to [check_codebase.sh](../check_codebase.sh) between step 3 (format:check) and step 4 (test:node, renumbered to 5); header step list updated to reflect 7 steps total. First-pass lint reported 337 findings across 13 rules; underscore-pattern config dropped no-unused-vars to 332; `npm run lint -- --fix` drained 216 mechanical findings (`consistent-type-imports`: 185, `no-unnecessary-type-assertion`: 22, `prefer-const`: 6, plus a handful of `no-unused-vars` adjustments), leaving 121 hand-triage findings. Hand-fixes: removed dead imports across `src/college/{college_core,college_entry,college_season_builder}.ts`, `src/core/year_handler.ts` (CareerPhase), `src/crisis.ts` (randomInRange), `src/events.ts` (CoreStats), `src/game_loop.ts` (applyWeeklyFocus, canDoActivity, unused `desc` text block), `src/high_school/hs_postgrad.ts` (countOffers), `src/high_school/recruiting_events.ts` (showSigningDay/showWalkOnOptions), `src/high_school/recruiting_offers.ts` (getSchoolsAtState, countOffers), `src/nfl_handlers/nfl_rookie.ts` (clampStat), `src/player.ts` (CareerPhase, DepthChartStatus from local-only import), `src/plugins/plugin_host.ts` (CareerContext), `src/plugins/scout_report/scout_report_logic.ts` (clampStat), `src/recruiting.ts` (OfferState, getSchoolById, AcademicStanding, CoreProgress), `src/simulator/adapter.ts` (PositionBucket), `src/simulator/models/play_result_model.ts` (Situation), `src/tab_manager.ts` (switchTab), `src/team.ts` (clampStat), `src/ui/{header_widget,stats_widget,story_widget}.ts` (CareerPhase/getElement/findElement), `src/weekly/choices.ts` (clampStat, randomInRange), `src/weekly/season_lifecycle.ts` (Activity, EngineState), `src/weekly/week_phases.ts` (WeeklyChoice, ChoiceResult), `src/weekly/weekly_engine.ts` (WeeklyChoice, ChoiceResult, CrisisDefinition), `tools/sim_conference_season.ts` (SeasonGame, AggregateStats). Underscore-prefixed intentionally-unused params: `_player` in `src/activities.ts:getActivitiesForPhase`, `src/nfl.ts:getNFLMidseasonEvent` (also `_year`), `src/nfl_handlers/nfl_season_builder.ts:buildNFLSchedule` (`_playerTeamId`), `src/recruiting.ts:computeSchoolFit`; `_ctx` across `src/plugins/{childhood,college,high_school,nfl}/tabs.ts` and `src/plugins/childhood/panels/career_panel.ts`; `_homeContext` in `src/simulator/adapter.ts:convertToGameResult`; `_tuning` in `src/simulator/engine/game_engine.ts:resolvePat` and `src/simulator/models/play_call_model.ts:choosePlay`; `_outcome` in `src/simulator/engine/rules_engine.ts:applySafety`; `_secsRemaining` in `src/simulator/models/play_call_model.ts:passProb`; `_state` in `src/simulator/models/play_result_model.ts:{resolveKneel,resolveSpike}`; `_teamScore`/`_opponentScore`/`_isHome` in `src/simulator/output/story_summary.ts:analyzeScoreTrends`. Real `no-floating-promises` fixes (the high-signal target rule the user wanted): `src/main.ts` line 183 (`void initGame()` inside `onRestart` callback) and line 288 (`action: () => { void initGame(); }` replacing the bare `action: initGame` reference); the `no-misused-promises` site at 288 is fixed by the same wrap. Real `no-unsafe-assignment` fixes: `src/recruiting.ts` cluster of 12 sites -- `Array(n).fill('foo')` returns `any[]` because `fill` has no inferred element type when `Array` is called untyped; changed every site to `Array<string>(n).fill(...)` so the array is typed `string[]`. Real `restrict-template-expressions` fixes: `src/tabs.ts:87` (`${String(phase)}` in unreachable-default error) and `tests/test_choice_schemas.ts:105` (wrap `unknown` probabilities in `String(...)`). Real `no-base-to-string` fix: `src/save/validate.ts:47` -- switched from `String(envelope.schemaVersion)` (where `schemaVersion: unknown` would stringify object as `[object Object]`) to `JSON.stringify(envelope.schemaVersion)` which is safe for any unknown shape. Eslint-disables with justification (3 sites only): `src/simulator/engine/game_engine.ts` lines 105-117: `/* eslint-disable @typescript-eslint/no-unsafe-enum-comparison -- defensive runtime guard, see comment */` wrapping the quarter-end check because removing the `(state.phase as string)` casts broke typecheck (TS narrowed the union to exclude `Phase.GAME_OVER` upstream, but runtime phase may still be that value at this branch); `src/childhood/name_loader.ts` line 98, `src/ncaa.ts` line 72, `src/nfl.ts` line 40, `src/weekly/choices.ts` line 73 (`// eslint-disable-next-line @typescript-eslint/require-await -- M5 loader contract: callers await; body is sync because csv text / JSON is bundled at build time`) preserving the async signature on M5 bundle-time loaders; `tests/test_handler_registry.ts` lines 67-70 (three `// eslint-disable-next-line @typescript-eslint/unbound-method -- truthiness check, method is not invoked` on the `assert.ok(obj.method, ...)` existence checks). Test file housekeeping: `tests/test_handler_registry.ts` `withFreshRegistry` and 3 test functions converted from spurious-`async` to sync; `tests/test_plugin_host.ts` `main()` made sync (no awaits inside), the 10 unused `catch (err)` instances renamed to `catch (_err)`. After every fix and `npm run format:write` to reconcile prettier with eslint-fix outputs, `npm run lint` exits 0; `bash check_codebase.sh --fast` summary shows `[PASS] typecheck`, `[PASS] typecheck:lint`, `[PASS] format:check`, `[PASS] lint`, `[PASS] test:node`, `[SKIP] test:playwright (--fast)`, `[SKIP] build (--fast)`; full `bash check_codebase.sh` summary shows the same five PASS + `[SKIP] test:playwright (no playwright specs found)` + `[PASS] build`, totaling 7 PASS / 1 SKIP, bundle `dist/main.js` 507.4kb (unchanged from M9 baseline). Plan reference: `~/.claude/plans/dynamic-tinkering-avalanche.md` M-A. M-B (test paradigm conversion to `node:test`) is the next milestone and depends on this one so the new test files get linted under the new ESLint config.
- **Install/usage doc stubs added (`install-usage-docs`)**: created [docs/INSTALL.md](INSTALL.md) and [docs/USAGE.md](USAGE.md) as minimal stubs covering prerequisites, `bash devel/setup_typescript.sh` (updated from initial `setup_game.sh` reference by the M1 rename later the same day), optional `bash devel/setup_playwright.sh`, `bash run_web_server.sh`, `npm run dev` / `npm run build`, `npm test`, and `bash check_codebase.sh`. Both files link out to deeper docs (CODE_ARCHITECTURE, FILE_STRUCTURE, PLUGIN_ARCHITECTURE, E2E_TESTS, PLAYWRIGHT_USAGE) rather than restating their content; USAGE.md flags save-data location as an unverified Known gap.
- **Milestone M6-A: Scout Report optional plugin (panel-only proof)**: moved `archive/disconnected_features/scout_report.ts` to `src/plugins/scout_report/scout_report_logic.ts` via `git mv` and rebind imports from archive-relative `./player.js` to repo path `../../player.js` (Player and clampStat now imported from src/player.ts). Created `src/plugins/scout_report/index.ts` exporting `scoutReportPlugin: GamePlugin` with DOM-free `register(host)` calling `host.ui.registerPanel(scoutReportPanel)` only (no event hooks, no lifecycle hooks). Created `src/plugins/scout_report/panels/scout_report_panel.ts` with PanelRegistration and `render(ctx: GameContext)` that reads player via `ctx.getPlayer()`, calls `generateScoutReport() + formatScoutReport()`, and mounts HTML into panel-content slot. Panel available in college and NFL phases (draft-stock relevant phases). Updated `src/plugins/register_plugins.ts` to import scoutReportPlugin and call `scoutReportPlugin.register(host)` as the fifth plugin registration (after four phase plugins: childhood, high_school, college, nfl). Removing the one registration line disables the plugin with zero other code change. Proof checkpoint VERDICT: PROCEED -- panel-only registration pattern proven modular; DOM boundary (index.ts is DOM-free, panels/ holds render only) proven cleanest. No cross-plugin imports, no `as any`, all types strict.

- **Milestone M3-A: Childhood plugin vertical slice complete**: `src/plugins/childhood/` now ships the full vertical slice (3 phase handlers ages 1-13: kid_years, peewee, travel; activities loader + JSON; tabs (hidden in UI but registered for consistency); career panel (minimal status-only); events loader + JSON combining all 9 age-specific event files; and 2 lifecycle hooks: age-5 first-football experience and childhood entry phase start). Plugin's `register(host)` in `src/plugins/register_plugins.ts` is the sole boot site for childhood feature content. Updated `src/core/register_handlers.ts` to delete all childhood handler imports and registrations; handlers now registered via childhoodPlugin only. Updated `src/activities.ts` to route `childhood` phase to pluginHost alongside `high_school` and `nfl`. Updated `src/tabs.ts` to route `childhood` tabs via pluginHost (tabs hidden in phase-display logic but registered). Updated `src/main.ts` to add preload calls for `preloadChildhoodActivities()` and `preloadChildhoodEvents()` in bootstrap sequence. Consolidated 9 age-specific event JSON files (`src/data/events/childhood_1.json` through `childhood_9.json`) into single `src/plugins/childhood/events/childhood.json` via Python merge. Added 2 lifecycle hooks: `childhood-age-5-first-football` (AgeHook fires once at age 5 with narrative about first football interest), `childhood-entry` (PhaseStartHook fires at childhood phase start with game-start headline). Childhood panel is minimal; no team, no recruiting, no clutch pools. No cross-plugin imports, no `as any`, all types strict. Verification: TODO pending nfl/college stubs removal (scaffolds created by linter, not part of M3-A scope).

- **Milestone M3-B: College plugin vertical slice complete**: `src/plugins/college/` now ships the full vertical slice (3 phase handlers ages 18-21, activities loader + JSON, tabs, career panel, events loader + JSON, and 2 lifecycle hooks: age-20 NFL declaration and college entry phase start). Plugin's `register(host)` in `src/plugins/register_plugins.ts` is the sole boot site for college feature content. Updated `src/core/register_handlers.ts` to delete all college handler imports and registrations; handlers now registered via collegePlugin only. Updated `src/activities.ts` to remove `COLLEGE_ACTIVITIES` constant; getActivitiesForPhase() routes college phase to pluginHost. Updated `src/tabs.ts` to route college tabs via pluginHost. Updated `src/main.ts` to add preload calls for `preloadCollegeActivities()` and `preloadCollegeEvents()` in bootstrap sequence. Moved college events JSON from `src/data/events/college.json` to `src/plugins/college/events/college.json`. Added 2 lifecycle hooks: `college-age-20-nfl-declaration` (AgeHook fires once at age 20 to notify about early NFL declaration option), `college-entry` (PhaseStartHook fires at college phase start with narrative context). No cross-plugin imports, no `as any`, all types strict.

- **Milestone M3-C: NFL plugin vertical slice complete**: `src/plugins/nfl/` now ships the full vertical slice (5 phase handlers ages 22-39, activities loader + JSON, tabs, career panel, events loader + JSON, and 3 lifecycle hooks: age-22 draft day, NFL entry phase start, retirement). Plugin's `register(host)` in `src/plugins/register_plugins.ts` is the sole boot site for NFL feature content. Updated `src/core/register_handlers.ts` to delete all NFL handler imports and registrations; handlers now registered via nflPlugin only. Updated `src/activities.ts` to remove `NFL_ACTIVITIES` constant; getActivitiesForPhase() routes NFL phase to pluginHost. Updated `src/tabs.ts` to route NFL tabs via pluginHost like HS. Updated `src/main.ts` to add preload calls for `preloadNflActivities()` and `preloadNflEvents()` in bootstrap sequence before `registerAllPlugins(host)`. Moved NFL events JSON from `src/data/events/nfl.json` to `src/plugins/nfl/events/nfl.json` (via cp). Added 3 lifecycle hooks: `nfl-age-22-draft-day` (AgeHook fires once at age 22), `nfl-entry` (PhaseStartHook fires at NFL phase start), `nfl-retirement` (CareerEndHook fires at retirement trigger). No cross-plugin imports, no `as any`, all types strict.

- **Milestone M2 EXIT: high school plugin vertical slice complete**: `src/plugins/high_school/` now ships the full vertical slice (phase handlers, activities loader + JSON, tabs, career panel, events loader + JSON, and 2 lifecycle hooks). The plugin's `register(host)` call in `src/plugins/register_plugins.ts:registerAllPlugins(host)` is the sole boot site for HS feature content. Proof checkpoint VERDICT: PROCEED -- adding a new HS feature now edits 1 plugin tree instead of 5 scattered files (`src/activities.ts` + `src/tabs.ts` + `src/ui/career_widget.ts` + handler file + events). Plan continues to M3 (childhood, college, NFL phase plugins).
- **High School plugin (M2-D): migrated events and lifecycle hooks to PluginHost registration**: moved HS event JSON file from `src/data/events/high_school.json` to `src/plugins/high_school/events/high_school.json` via `git mv` to preserve history. Created `src/plugins/high_school/events_loader.ts` that fetches and parses HS events, returning `GameEvent[]`, mirroring the pattern from activities_loader.ts. Created two HS lifecycle hooks: (1) `drivers_permit.ts`: AgeHook fired once at age 15 with narrative text noting driver's permit eligibility; (2) `hs_entry.ts`: PhaseStartHook fired at HS phase start with headline "You're a High School Freshman" and narrative text. Created `src/plugins/high_school/lifecycle/hooks.ts` that registers both hooks via `host.lifecycle.registerAgeHook(driversPermitHook)` and `host.lifecycle.registerPhaseStartHook(hsEntryHook)`. Updated `src/plugins/high_school/index.ts` to call event loader registration via `host.events.registerMany(loadHsEvents())` and lifecycle registration via `registerHsLifecycleHooks(host)`. Updated `src/events.ts` loader to skip 'high_school' from the phases array since HS events are now pre-registered by the plugin. HS weekly choices remain arc-phase-scoped (preseason/opening/midseason/stretch/postseason) and are NOT moved; choices are shared across all career phases. Loaders handle both browser fetch environment (`/src/plugins/...json`) and test environment (fallback inline data). Boot order: `src/main.ts` calls `await preloadHsActivities()` and `await preloadHsEvents()` before `registerAllPlugins(host)` to ensure plugin registration is synchronous. Tests updated: `test_handler_registry.ts` and `test_plugin_host.ts` now call preload functions before `registerAllPlugins()`. Verification: `bash check_codebase.sh` all tests green, `timeout 240 node tests/playwright/autoplay.mjs` reaches age 24 (past HS years at ages 14-17) with no errors, Hall of Fame detected.

### Behavior or Interface Changes

- **Milestone M9: documentation refresh for the esbuild + dist-only migration**: refreshed [README.md](../README.md), [docs/INSTALL.md](INSTALL.md), [docs/USAGE.md](USAGE.md), [docs/FILE_STRUCTURE.md](FILE_STRUCTURE.md), and [docs/CODE_ARCHITECTURE.md](CODE_ARCHITECTURE.md) to match the post-M8 reality. README quick-start now points at `bash devel/setup_typescript.sh`, describes the self-contained `dist/` artifact and `bash run_web_server.sh`, and replaces the legacy `npm test` reference with the canonical pre-push gate `npm run check` (alias for `bash check_codebase.sh`) plus the `--fast` and `--skip-playwright` flag notes. INSTALL.md mentions esbuild 0.28, ESLint 10, and Prettier 3 alongside the existing TypeScript/tsx/Playwright deps, describes the self-contained dist artifact (`index.html`, `main.js`, `main.js.map`, `styles/`, `.nojekyll`), and uses `npm run check` for verification. USAGE.md drops the obsolete `file://` open-`index.html` path and the tsc-watch dev description, documents the random-port `run_web_server.sh` flow, clarifies that `--fast` is passed to the shell script directly (not via `npm run check --`), and adds a Known gap noting the pre-existing `tests/test_handler_registry.ts` baseline failure plus the `gridiron_life_save` localStorage key. FILE_STRUCTURE.md root tree drops `index.html` and `styles.css` (both moved or deleted in M3), adds `.prettierrc.json`/`.prettierignore`, and adds a new `dist/` subsection enumerating the self-contained artifact contents; resolved Known gaps are dropped; an `_site/` untracked leftover entry is added. CODE_ARCHITECTURE.md gains a new Build section describing the esbuild pipeline (typecheck via tsc, bundle via esbuild with `--loader:.csv=text --loader:.json=json`, copy `src/index.html` + `src/styles/`, write `.nojekyll`) and clarifies that plugin JSON, weekly choice JSON, and the name/team CSVs are esbuild-resolved imports rather than runtime fetches; the Testing section now describes the smart orchestrator (preflight checks, fail-fast ordering, `--fast`/`--skip-playwright` flags, PASS/FAIL summary). All edits hold to the file-path-link-text rule from [docs/MARKDOWN_STYLE.md](MARKDOWN_STYLE.md). Verification: `npx prettier --write` on the six docs idempotently formats them; `npm run format:check` exits 0; `bash check_codebase.sh --fast` summary shape unchanged (4 PASS: typecheck, typecheck:lint, lint, format:check; 1 FAIL: test:node on the pre-existing `tests/test_handler_registry.ts` baseline -- documented in USAGE.md Known gaps and CODE_ARCHITECTURE.md Known gaps as outside M1-M9 scope). `_site/` untracked leftover at the repo root from the pre-M4 staging layout is documented as cleanup-pending; the user can `rm -rf _site` at any time. M9 closes the M1..M9 plan defined in `~/.claude/plans/dynamic-tinkering-avalanche.md`.

- **Milestone M7: adopt smart check_codebase.sh orchestrator from starter-repo-template**: replaced the legacy 22-line `check_codebase.sh` (which only ran `tsc -p tsconfig.lint.json` + `tsx tests/run.ts` and exited 0 even with test failures) with the smart orchestrator copied from `~/nsh/starter-repo-template/templates/typescript/check_codebase.sh`. Switched shebang from `#!/bin/sh` to `#!/usr/bin/env bash` (bash arrays + `set -euo pipefail`). Pipeline runs `npm run --silent <name>` in fixed order: typecheck -> typecheck:lint (only if tsconfig.lint.json present) -> lint -> format:check -> test:node -> test:playwright -> build. Per-step PASS/FAIL/SKIP tracking via parallel bash 3.2-compatible arrays; EXIT trap prints summary; first failing step exits 1 immediately so later steps don't mask earlier failures. Preflight: validates `node`, `npm`, `package.json`, `node_modules/`, and warns (not fails) on missing `package-lock.json`. Flags: `--fast` skips both Playwright and build; `--skip-playwright` skips Playwright only; `-h|--help` prints usage. Two repo-specific adaptations from the starter version: (1) post-build artifact assertion checks `dist/index.html`, `dist/main.js`, and `dist/styles/base.css` -- the starter assumes a single `src/style.css` / `dist/style.css`, but this repo uses a `src/styles/*.css` 10-file subtree copied to `dist/styles/`, with `base.css` the canonical anchor; (2) Playwright detection triggers if `tests/playwright/autoplay.mjs` exists OR if any `*.spec.{ts,mjs}` exists under `tests/playwright/` -- the starter's spec-glob-only detection would silently skip this repo's existing `autoplay.mjs` entry. Final summary line is now "PASS: N checks (0 failed)." or "FAIL: M of N checks failed.", upgrading from the starter's plain "M of N checks failed." Verification: `bash check_codebase.sh --help` exits 0 and prints usage; `bash check_codebase.sh --fast` exits 1 stopping at the pre-existing lint failure (1667 ESLint problems including 160 errors against `dist/main.js` -- eslint config is scanning the bundled output; out of M7 scope) with Playwright + build correctly skipped; full `bash check_codebase.sh` and `--skip-playwright` runs both fail-fast at the same lint step (test:node and the pre-existing `tests/test_handler_registry.ts` baseline failure never reached). The new orchestrator correctly surfaces every failure loudly instead of hiding test:node failures behind `exit 0`. See `~/.claude/plans/dynamic-tinkering-avalanche.md` for the full M1..M9 plan.

- **Milestone M6: rewrite run_web_server.sh (dist-only) and delete legacy build**: rewrote `run_web_server.sh` to match the starter-repo-template typescript template's dist-only model. New flow: `set -euo pipefail`, `cd "$(git rev-parse --show-toplevel)"`, assert `node_modules/` exists (error message now points at `npm install` or `bash devel/setup_typescript.sh` per M1 rename), run `./build_github_pages.sh` so `dist/` is fresh, pick a random port `PORT="${PORT:-$((8000 + RANDOM % 1000))}"` (each port is its own browser origin, so the cache is effectively invalidated every run; `PORT=` env var overrides), open the browser via `(sleep 1 && open ...) &` when `open` is available and stdin is a tty, then `python3 -m http.server "${PORT}" --directory dist`. Dropped the legacy cache-bust perl invocation that rewrote `src/index.html`'s `<script src=/dist/main.js?v=N>` query (no longer applicable now that dist is self-contained and the random port handles cache invalidation). Removed `build:legacy` npm script from `package.json` (introduced in M2 as a temporary rollback path until M5 lands; M5 has shipped). Replaced `dev` script: was `tsc --watch` (useless now that esbuild owns output), now `esbuild src/main.ts --bundle --format=esm --target=es2020 --sourcemap --loader:.csv=text --loader:.json=json --outfile=dist/main.js --watch` (no `--minify` so dev rebuilds stay fast and produce readable output for debugging). Cleaned `tsconfig.json`: removed `"outDir": "./dist"` (dead now that esbuild owns output and `tsc --noEmit` runs everywhere); kept `"rootDir": "./src"` (still useful for scoping tsc type-checking). Verification: `bash run_web_server.sh` with `PORT=8456` rebuilds dist/ (bundle = 509K minified), starts the server, and serves `/` (HTTP 200), `/main.js` (returns minified JS starting `var We="gridiron_life_save";...`), and `/styles/base.css` (returns the base CSS). `npm run typecheck` exits 0. `npm run test:node` exits with the same pre-existing `tests/test_handler_registry.ts` baseline failure (no regression from M5). `npm run build:legacy` now fails with `Missing script: "build:legacy"` (confirming removal). `npm run dev` starts esbuild --watch and does the initial build; a one-shot `npx esbuild ... --outfile=/tmp/_dev_main.js` (no `--watch`, no `--minify`) confirms the dev script's flag combination produces a working 804K non-minified bundle. See `~/.claude/plans/dynamic-tinkering-avalanche.md` for the full M1..M9 plan.

- **Milestone M5: convert JSON and CSV loaders to bundle-time imports**: replaced every runtime `fetch()` call to a `/src/...json` URL or a relative `src/data/*.csv` URL with a static ES module import so esbuild inlines the data into `dist/main.js`. Touched 7 JSON loaders (`src/plugins/{childhood,college,high_school}/{activities,events}_loader.ts`, `src/plugins/high_school/packs/example_pack_loader.ts`), 2 NFL loaders (`src/plugins/nfl/{activities,events}_loader.ts`, which were inlining a different/sparse copy of the data -- promoted to JSON imports so the canonical `src/plugins/nfl/activities.json` and `src/plugins/nfl/events/nfl.json` are the only source; NFL events grew from 2 inline entries to 30+ JSON entries), `src/weekly/choices.ts` (5 static phase imports replace the template-literal fetch loop; `loadChoicePools` now copies bundled data into the existing mutable `choicePools` record so the async API and downstream code stay unchanged), and 3 CSV loaders (`src/childhood/name_loader.ts`, `src/ncaa.ts`, `src/nfl.ts`). Exported function signatures unchanged (`fetchFoo` / `preloadFoo` / `loadFoo` patterns preserved) so no caller in `src/main.ts`, plugin registrars, or tests needs to be touched. Inferred JSON literal types are too narrow for the destination interfaces (`Activity`, `GameEvent`, `WeeklyChoice`, `DataPack` use `Record<string, number>` for effects but the JSON narrows to specific keys), so each import is cast through `as unknown as Foo[]` with an explaining comment. Added `src/csv_modules.d.ts` declaring `*.csv` imports as `string` to match esbuild's `--loader:.csv=text` contract. Added `tests/fixtures/csv_loader.mjs`, a self-registering Node ESM loader hook that reads `.csv` imports as UTF-8 text under tsx (browser bundle never hits this path -- esbuild handles CSVs at build time); wired into `tests/run.ts` via `npx tsx --import <hook> ...` so every test child process gets the hook installed before any user code runs. Reduced `tests/fixtures/fetch_mock.ts` to a no-op stub: the JSON URL map is dead now that imports happen at bundle time, but `test_handler_registry.ts` and `test_plugin_host.ts` still import `setupFetchMock`, so the file's public surface stays. Verification: no `fetch('/src` or `fetch('src/data` references remain in `src/**/*.ts`; `npm run typecheck` exits 0; `npm run test:node` exits 0 with the same pre-existing `tests/test_handler_registry.ts` baseline failure (the test does not preload childhood/college/NFL before `registerAllPlugins`, predating M5); `bash build_github_pages.sh` succeeds; `wc -c dist/main.js` = 520962 bytes (~509KB minified, up from ~320KB pre-M5 because the JSON/CSV payloads are now inlined into the bundle); `python3 -m http.server --directory dist 8766` serves `/` with HTTP 200. See `~/.claude/plans/dynamic-tinkering-avalanche.md` for the full M1..M9 plan.

- **Milestone M4: rewrite build_github_pages.sh as the esbuild build (dist-only artifact)**: rewrote `build_github_pages.sh` to drop the legacy `_site/` repo-root-mirror staging and produce a self-contained `dist/` artifact. New flow: pre-flight checks (`node_modules/`, `src/main.ts`, `src/index.html`, `src/styles/`), wipe `dist/` from scratch, run `npx tsc --noEmit -p tsconfig.json` for type-check only (esbuild owns output), bundle `src/main.ts` -> `dist/main.js` via `npx esbuild --bundle --format=esm --target=es2020 --minify --sourcemap --loader:.csv=text --loader:.json=json`, `cp src/index.html dist/index.html`, `cp -R src/styles dist/styles`, perl in-place rewrite of `dist/index.html` ONLY (never touches `src/index.html`) to convert the absolute script tag `/dist/main.js` (with optional `?v=N` query) -> relative `main.js` and CSS link hrefs `/src/styles/foo.css` -> `styles/foo.css`, write `dist/.nojekyll`, and assert `dist/index.html`, `dist/main.js`, and `dist/styles/base.css` all exist. `tsconfig.json` already had `resolveJsonModule: true` set (no change needed). CSS is NOT bundled through esbuild; it stays as static `<link>` tags pointing at the copied subtree. Verification: `bash build_github_pages.sh` exits 0 ("Built dist/ (esbuild, self-contained, GitHub Pages-ready)."); `ls -la dist/` shows `.nojekyll`, `index.html`, `main.js`, `main.js.map`, `styles/` (no `.ts` files); `ls dist/styles/ | wc -l` returns 10; rewritten script tag is `<script type="module" src="main.js"></script>`; rewritten CSS link is `<link rel="stylesheet" href="styles/base.css">`; bundle size `wc -c dist/main.js` = 327250 bytes (~320KB minified); `npm run typecheck` exits 0; `python3 -m http.server --directory dist 8765` serves `/` with one `main.js` reference and `/main.js` returns minified JS starting `var We="gridiron_life_save";...`. Plugin loaders that still fetch `/src/...json` will FAIL when served from `dist/` alone -- that is expected and is M5's job; this milestone only proves the bundle exists and the script tag resolves. See `~/.claude/plans/dynamic-tinkering-avalanche.md` for the full M1..M9 plan.

- **Milestone M3: move index.html under src/ (esbuild migration prep)**: `git mv index.html src/index.html` so the canonical entry HTML now lives with the other source under `src/`. CSS `<link>` hrefs and the `<script>` tag inside `src/index.html` were switched from `src/styles/foo.css` + `dist/main.js` to absolute paths `/src/styles/foo.css` + `/dist/main.js`. Reason for the absolute-path deviation from the plan-stated relative paths: the legacy build still serves the repo root, so `src/index.html` must resolve siblings (`src/styles/`) and the build artifact (`dist/main.js`) from the same docroot. Plan's `main.js`-only script tag assumes the M4 future where `dist/index.html` sits next to `dist/main.js`; absolute `/dist/main.js` works in both the M3 legacy layout and the M4 future. `git rm styles.css` from the repo root; the 2026-05 changelog entry U-12 already documented it as unused (no HTML referenced it; `src/theme.ts` only mentioned it in a comment), and `src/styles/*.css` is the canonical CSS tree. `build_github_pages.sh` surgical update: `cp index.html _site/index.html` -> `cp src/index.html _site/index.html`; cache-bust regex `dist/main\.js` -> `/dist/main\.js` to match the new absolute-path script tag. `run_web_server.sh` surgical update: cache-bust now rewrites `src/index.html` in-place (was `index.html`), regex updated to match `/dist/main.js`, and the browser-open URL changed from `http://localhost:${PORT}/` to `http://localhost:${PORT}/src/` so the dev server lands directly on the new entry. Did NOT rewrite `build_github_pages.sh` or `run_web_server.sh` as esbuild/dist-only -- those are M4/M6 scope. Verification: `git ls-files index.html src/index.html` shows only `src/index.html`; `bash build_github_pages.sh` exits 0 and prints "Built dist/ + staged \_site/ (repo-root Pages model)."; `python3 -m http.server` against the repo root returns 200 for `/src/`, `/src/index.html`, `/dist/main.js`, and `/src/styles/base.css`; cache-bust applies to `_site/index.html` (script tag becomes `/dist/main.js?v=<timestamp>`); `npm run typecheck` exits 0; `npm run test:node` exits 0 with the same pre-existing `tests/test_handler_registry.ts` baseline failure (not regressed). See `~/.claude/plans/dynamic-tinkering-avalanche.md` for the full M1..M9 plan.

- **Milestone M2: npm script graph + Prettier config (esbuild migration prep)**: added the canonical npm script graph to `package.json` matching the starter-repo-template typescript template. New scripts: `typecheck` (`tsc --noEmit -p tsconfig.json`), `typecheck:lint` (`tsc --noEmit -p tsconfig.lint.json`), `lint` (`eslint .`), `format:check` (`prettier --check .`), `format:write` (`prettier --write .`), `test:node` (`tsx tests/run.ts`), `test:playwright` (`node tests/playwright/autoplay.mjs`), `build` (`./build_github_pages.sh` -- will be rewritten in M4), `build:legacy` (`tsc -p tsconfig.json` -- temporary rollback path until M6 removes it), `serve` (`./run_web_server.sh`), `check` (`./check_codebase.sh`). Kept `dev` (`tsc --watch`) for compatibility; removed the old `build` (`tsc`), `lint` (was wrongly mapped to tsc lint), and `test` (replaced by `test:node`). Added `.prettierrc.json` (useTabs:true, singleQuote:true, printWidth:100, semi:true, trailingComma:es5) per [docs/TYPESCRIPT_STYLE.md](TYPESCRIPT_STYLE.md). Added `.prettierignore` covering `node_modules/`, `dist/`, `dist-single/`, `archive/`, `package-lock.json`. Did NOT touch `build_github_pages.sh`, `run_web_server.sh`, `check_codebase.sh`, `tsconfig.json`, `tsconfig.lint.json`, or `eslint.config.js`; legacy build remains the canonical path until M4. Did NOT run `npm run format:write` -- M8 is the isolated Prettier-only commit. Residual: `npm install --save-dev esbuild prettier eslint` is the remaining install step blocked behind user approval; once run, `npm run lint` and `npm run format:check` will execute. `npm run typecheck` exits 0; `npm run test:node` exits 0 with the pre-existing `tests/test_handler_registry.ts` baseline failure (not regressed); `npm run build:legacy` succeeds; `bash check_codebase.sh` still produces the same baseline output. See `~/.claude/plans/dynamic-tinkering-avalanche.md` for the full M1..M9 plan.

- **Milestone M1: setup-script rename (esbuild migration prep)**: `git mv setup_game.sh devel/setup_typescript.sh` to match the starter-repo-template typescript template layout. `git rm setup_playwright.sh` from the repo root; `devel/setup_playwright.sh` is now the sole copy. Final hint in `devel/setup_typescript.sh` rewritten to point at `bash run_web_server.sh`, `bash check_codebase.sh` (lint gate today, `npm run check` once package.json scripts land in M2), and `bash devel/setup_playwright.sh`. Updated `devel/setup_playwright.sh` header comment to reference `devel/setup_typescript.sh` instead of the deleted root `setup_game.sh`. Doc updates: [README.md](../README.md) quick-start gains `bash devel/setup_typescript.sh`; [docs/INSTALL.md](INSTALL.md) setup and Playwright sections updated to the new paths; [docs/FILE_STRUCTURE.md](FILE_STRUCTURE.md) drops the root `setup_game.sh`/`setup_playwright.sh` entries, adds `setup_typescript.sh` under the `devel/` listing, and removes the resolved "root setup_playwright.sh duplicate" known-gap bullet. M1 does NOT change the build model; `bash check_codebase.sh` still runs the legacy tsc-emit path. Residual: `build_github_pages.sh`, `check_codebase.sh`, and `run_web_server.sh` still mention `./setup_game.sh` in their `node_modules missing` error messages; those three scripts are rewritten in M4/M6/M7 and are out of M1 scope. See `~/.claude/plans/dynamic-tinkering-avalanche.md` for the full M1..M9 plan.

- **WP-M2-D concern #1 resolved: activities.json is now authoritative source**: `src/plugins/high_school/activities_loader.ts` was previously hardcoding the same 5 activities as in `activities.json`. Refactored to fetch from JSON file at runtime (browser) or use fallback data in tests. Loader signature changed to async `fetchHsActivities()` with sync wrapper `loadHsActivities()` that returns pre-loaded data. Plugin no longer has duplicate source-of-truth problem; JSON is canonical. Activities data is identical, ensuring no behavior change.

- **WP-M2-D concern #2 resolved: tab render stubs marked for deferred wiring**: added TODO comments above each of the 6 tab render callbacks in `src/plugins/high_school/tabs.ts` (life, stats, activities, team, career, social tabs) referencing "M3 tab render wiring" to indicate deferred implementation per plan. No behavior change; render bodies remain `// Implemented by UI layer` stubs pending M3 migration.

- **M2 aggregate behavior change summary**: HS_ACTIVITIES removed from `src/activities.ts`; HS rows removed from `src/tabs.ts:PHASE_TABS`; `renderHSCareer` removed from `src/ui/career_widget.ts`. All three subsystems now query `PluginHost` for HS data via `setPluginHost(host)`, `setTabsPluginHost(host)`, and `setCareerPluginHost(host)`. `src/main.ts` wires the three setters AFTER `registerAllPlugins(host)`. Boot order is locked: core handlers, then plugin registration, then setter wiring.

### Fixes and Maintenance

- **Audit cleanup pass 2: applied audit-flagged stale-comment and stale-doc fixes**: rewrote the stale header in `build_github_pages.sh` (old text claimed plugin loaders still fetch `/src/...` -- false post-M5 since loaders import at bundle time); replaced the misleading `src/index.html:15` comment about "M4 will switch to relative when dist/ becomes self-contained" (never happened by design -- `build_github_pages.sh` perl-rewrites `dist/index.html` to relative paths so `src/index.html` can stay absolute for the dev server); corrected the `check_codebase.sh` header comment about Playwright detection (header claimed `autoplay.mjs` OR `*.spec.{ts,mjs}`, but `playwright_has_entry()` only matches `*.spec.{ts,mjs}` -- autoplay.mjs is a standalone manual smoke runner); removed the stale "Known gap" bullet about `tests/test_handler_registry.ts` currently failing from both `docs/USAGE.md` and `docs/CODE_ARCHITECTURE.md` (the test now passes post-M5 dead-code cleanup); added a single Testing-section line in `docs/CODE_ARCHITECTURE.md` documenting `tests/fixtures/csv_loader.mjs` as the Node ESM loader hook that lets tsx-run tests import `.csv` files (esbuild handles the same mapping at build time) and dropped the now-orphan `tests/fixtures/fetch_mock.ts` bullet since that file was deleted earlier today; dropped the stale `(npm run check once package.json scripts land)` parenthetical from `devel/setup_typescript.sh:22` -- the scripts have landed; added `docs/CHANGELOG-*.md` to `.prettierignore` so Prettier no longer escapes underscores in historical archive entries; consolidated the 2026-05-21 day block so each subsection heading appears at most once. Verified `"main": "eslint.config.js"` was already removed from `package.json` by audit-fix-1. Verification: `bash check_codebase.sh --fast` summary remains `[PASS] typecheck / [PASS] typecheck:lint / [PASS] format:check / [PASS] test:node / [SKIP] test:playwright / [SKIP] build / PASS: 6 checks (0 failed).`; `npm run format:check` clean; `bash build_github_pages.sh` exits 0; `npm run dev` starts esbuild watch cleanly. Boundaries respected: no `src/` TypeScript, no test, no other build script touched.

- **Doc audit: refreshed stale [docs/ROADMAP.md](ROADMAP.md) Phase 5 and [docs/PLAYWRIGHT_USAGE.md](PLAYWRIGHT_USAGE.md) script template post-migration (audit R4)**: ROADMAP.md Phase 5 ("Build pipeline -- deferred until M6/M7 complete") described the exact esbuild + dist-only migration that shipped today across M1-M9; collapsed the ~33-line ASCII-diagram + tool-table + 6-step sequence into a single COMPLETE entry that points at [docs/CHANGELOG.md](CHANGELOG.md) and notes the optional `export_single_file.sh` wrapper as remaining future work (verified absent at repo root). PLAYWRIGHT_USAGE.md script template referenced a repo-root `index.html` (no longer exists post-M3) and a foreign `cell_culture_game.html` placeholder copied from a sibling repo; updated the canonical example to point at this repo's [tests/playwright/autoplay.mjs](../tests/playwright/autoplay.mjs), switched the URL from `file://` + `path.join(REPO_ROOT, 'index.html')` to `http://localhost:8000/index.html`, and added a prereq block noting `npm run build` plus `python3 -m http.server 8000 --directory dist` in a separate terminal. The repo-root shared-helper `repo_root.mjs` example was replaced with the inline `import.meta.url` + `path.resolve(__dirname, '..')` pattern that autoplay.mjs actually uses, since this repo does not ship `tests/playwright/repo_root.mjs`. The `cd /Users/vosslab/nsh/cell-culture-game-claude` example was replaced with this repo's path. Verification: `grep -n cell_culture docs/PLAYWRIGHT_USAGE.md` returns nothing; `grep -n 'Phase 5.*deferred' docs/ROADMAP.md` returns nothing.

- **Removed dead "preload gate" machinery from plugin loaders (post-M5 cleanup)**: the M5 migration replaced runtime `fetch()` with esbuild-resolved JSON imports, so the data is already available synchronously at module-import time. The loaders, however, retained their pre-M5 gate: a `loadedActivities: T[] | null = null` module variable, a `loadFoo()` getter that threw `"Foo not yet loaded. Call preloadFoo() during bootstrap."` when the gate was null, and a `preloadFoo()` async function that copied the import-time constant into the gate. Any caller that bypassed bootstrap (notably `tests/test_handler_registry.ts`, which exercises `registerAllPlugins(host)` directly without running `src/main.ts`'s preload chain) tripped the throw -- a real bug from leftover dead code, not a test problem. Fixed by rewriting every plugin loader under `src/plugins/**/*loader.ts` (9 files: `childhood/{activities,events}_loader.ts`, `college/{activities,events}_loader.ts`, `high_school/{activities,events}_loader.ts`, `high_school/packs/example_pack_loader.ts`, `nfl/{activities,events}_loader.ts`) so `loadFoo()` returns the import-time constant directly and `preloadFoo()` is a no-op kept for boot-order compatibility with `src/main.ts`'s 9 `await preload*()` calls (the imports remain so removing the bootstrap lines is a separate cleanup if desired). NFL loaders also had a lazy-init `ensureNflActivitiesLoaded` cache pattern that has been collapsed into the same direct-return shape for uniformity. `src/childhood/name_loader.ts` and `src/weekly/choices.ts` were checked but do not use this gate pattern, so no change. Verification: `npx tsx --import ./tests/fixtures/csv_loader.mjs tests/test_handler_registry.ts` now prints `handler_registry: 3/3 ok` (was failing with "not yet loaded"); `npm run test:node` reports `run.ts: all 9 test modules passed.` with no `1 test module(s) failed` line; `npm run typecheck` exits 0; `bash check_codebase.sh --fast` summary: `[PASS] typecheck / [PASS] typecheck:lint / [PASS] lint / [PASS] format:check / [PASS] test:node / [SKIP] test:playwright (--fast) / [SKIP] build (--fast) / PASS: 7 checks (0 failed).` (full green on the fast path); `bash build_github_pages.sh` exits 0 and produces a 507.6kb `dist/main.js`; full `bash check_codebase.sh` shows test:node PASS and only `test:playwright` FAIL on the pre-existing `ERR_CONNECTION_REFUSED at http://localhost:8000/index.html` autoplay baseline (unrelated to this fix; the autoplay harness expects an already-running dev server).

- **Milestone M8: Prettier first pass (isolated commit)**: ran `npm run format:write` to apply Prettier across the repo for the first time since `.prettierrc.json` landed in M2. Prettier reformatted 204 files in this pass (single-quote conversion, tab normalization, 100-char line wraps, `trailingComma: es5`); the diff is format-only with zero logic change, which is the entire point of keeping M8 isolated from the M1-M7 functional work. Verification: `npm run format:check` reports "All matched files use Prettier code style!"; `npm run typecheck` exits 0; `npm run test:node` exits with the same pre-existing `tests/test_handler_registry.ts` baseline failure (no new failures introduced); `bash build_github_pages.sh` produces a working `dist/main.js` at 520962 bytes (byte-identical to the pre-M8 bundle, confirming format-only-no-runtime-change); `bash check_codebase.sh --fast` summary now reads `[PASS] typecheck / [PASS] typecheck:lint / [PASS] lint / [PASS] format:check / [FAIL] test:node` -- format:check flips from FAIL (the M7.5 fail-fast point) to PASS, leaving test:node's pre-existing handler-registry baseline as the only red square. See `~/.claude/plans/dynamic-tinkering-avalanche.md` for the full M1..M9 plan.

- **Milestone M7.5: ignore build output, vendored deps, and archive in ESLint config**: rewrote `eslint.config.js` to invert the lint scope. Old config used `files: ['dist/**/*.js']`, which scoped rules to the bundled output and caused `npm run lint` (`eslint .`) to scan `dist/main.js` (the minified esbuild bundle), producing ~160 errors and ~1507 warnings (most of them `prefer-const`/`eqeqeq` complaints against minified, single-letter variable names). New config has a standalone flat-config ignores block (`{ ignores: ['dist/**', 'dist-single/**', '_site/**', 'node_modules/**', 'archive/**', 'OTHER_REPOS_FOR_STUDY/**'] }`) plus a `files: ['**/*.js', '**/*.mjs', '**/*.cjs']` block that keeps every existing rule (no-constant-condition, no-unreachable, no-duplicate-case, no-fallthrough, no-self-assign, no-self-compare, eqeqeq, no-var, prefer-const) unchanged. Source `.ts`/`.tsx` files are still owned by `tsc --strict` via tsconfig.json (no ESLint TypeScript parser pulled in); ESLint now only sees first-party `.js`/`.mjs`/`.cjs`, of which there are essentially none in repo source today, so `npm run lint` exits 0 cleanly. Added `_site/**` to the ignore list after the first lint pass surfaced the legacy M3-era staging tree at the repo root (left over before M4 wiped `_site/` from `build_github_pages.sh`); the directory is build output, not source. Verification: `npm run lint` exits 0 with no output (clean); `bash check_codebase.sh --fast` now passes typecheck, typecheck:lint, and lint, then fails-fast at format:check on the pre-existing "Code style issues found in 195 files" Prettier baseline (M8 scope) -- exactly the expected first-failure shape post-M7.5. test:node (which would surface the pre-existing `tests/test_handler_registry.ts` baseline failure documented in M3-A through M6) is not reached because format:check fails first; that ordering matches the new smart orchestrator's fail-fast contract from M7.

- **Arch docs refresh (`arch-docs`)**: refreshed [docs/CODE_ARCHITECTURE.md](CODE_ARCHITECTURE.md) (added HS post-grad / JUCO / recruiting helper files, named the surviving simulation tools, added check_plugin_boundaries.ts and fixtures/fetch_mock.ts to the testing section) and [docs/FILE_STRUCTURE.md](FILE_STRUCTURE.md) (added per-plugin `tabs.ts` entries, renamed childhood/college/nfl lifecycle files to current names, moved per-phase event JSON note out of `src/data/`, switched `src/data/choices/` to JSON-only, added root `styles.css` and `setup_playwright.sh` shims, removed nonexistent `LICENSE.CC_BY_4_0`, refreshed `tests/` and `tools/sim_conf/` trees, linked `PLUGIN_ARCHITECTURE.md` and `CHANGELOG-2026-05a.md`). Known gaps section now flags root `styles.css`, root `setup_playwright.sh`, and missing CC-BY license.

- **README refresh (`/readme-docs`)**: added `bash run_web_server.sh` as an alternative quick-start path alongside opening `index.html` directly; added a `## Testing` section with the verified commands `npm test` and `bash check_codebase.sh`; added `[docs/PLUGIN_ARCHITECTURE.md](docs/PLUGIN_ARCHITECTURE.md)` to the Developer reference list. No new docs created; quick start stays minimal so `docs/INSTALL.md` and `docs/USAGE.md` stubs remain unnecessary.

- **Test auditor findings applied (Reviewer 2)**: Fixed hardcoded absolute path in `tests/fixtures/fetch_mock.ts` (line 17) by replacing static `/Users/.../sports-life-game` with dynamic derivation using `url.fileURLToPath(import.meta.url)` and `path.resolve(path.dirname(here), '../..')` pattern (matches check_plugin_boundaries.ts). Replaced brittle `length === 1` count assertions in `test_plugin_host.ts` with `.some()` lookups (testLifecycleHookIndependence, testUiRegistryTabs/Panels/Widgets). Replaced post-clear `length === 0` assertions with `.find()` lookups on specific ids (testLifecycleClear, testUiRegistryTabs/Panels/Widgets clear). Deleted redundant phase handler assertions in testBootOrderNegativeNoHandlers; `phases.length > 0` check already verifies plugin registration. Removed dead-weight baseline file `tests/baselines/autoplay_M1_entry.json` via `git rm`. All changes reduce brittle size-dependent tests per PYTEST_STYLE.md ("avoid asserting on collection sizes"). Verification: tsc --noEmit (0 errors), test_plugin_host.ts (all pass), check_plugin_boundaries.ts (no violations), build_github_pages.sh (exit 0), autoplay smoke (reaches age 14+).

- **Fixed 9 markdown link errors in docs/CODE_ARCHITECTURE.md, docs/FILE_STRUCTURE.md, docs/CHANGELOG.md, and docs/CHANGELOG-2026-05a.md**: removed dead links to non-existent `src/core/register_handlers.ts` and `src/data/events/` directory (refactored away in M3); replaced with plain-text references to src/core modules and src/data. Fixed relative path in CHANGELOG.md cross-reference to CHANGELOG-2026-05a.md (was `../CHANGELOG-2026-05a.md`, now `CHANGELOG-2026-05a.md` per same-folder link rule). All 9 errors verified gone by `pytest tests/test_markdown_links.py`.

- **Age-22 unfinished-games season-advance bug fixed (WP-NFL-WEEK1)**: `simulateNonPlayerGames(activeEngine.season)` now runs before `advanceWeek()` in `src/weekly/season_lifecycle.ts:83` and defensively in `src/weekly/game_handler.ts:241`. Autoplay now progresses past the former age-22 crash point to age 24 with Hall of Fame detected.

- **Doc cleanup: FILE_STRUCTURE.md HS subtree refreshed to final M2 shape; PLUGIN_ARCHITECTURE.md worked example extended with events + lifecycle hook coverage. git added 4 previously-untracked HS plugin files (events_loader + 3 lifecycle/\*).**

- **WP-M2-D-FIX: removed silent-fallback try/catch blocks and HS_ACTIVITIES_FALLBACK / HS_EVENTS_FALLBACK hardcoded arrays from src/plugins/high_school/{activities_loader,events_loader}.ts**: loaders now throw on fetch failure per `no silent fallback` directive (TYPESCRIPT_STYLE.md lines 26-28, "Avoid broad try/catch blocks" and "Throw Error objects rather than returning silent failure values"). Deleted `HS_ACTIVITIES_FALLBACK` constant (lines 9-53 in activities_loader.ts) and `HS_EVENTS_FALLBACK` constant (lines 9-55 in events_loader.ts); both were re-introduced silent fallback problem violating original WP-M2-D brief instruction: "Remove the hardcoded TS array from activities_loader.ts. Add fetch error: throw Error with file path if response not OK (no silent fallback)." Refactored `fetchHsActivities()` and `fetchHsEvents()` to throw immediately on non-ok response with clear error message including URL and status code. If tests fail without real fetch environment, fix the tests, not the loaders (test framework must mock fetch or serve JSON locally).

- Rotated docs/CHANGELOG.md past 1000-line threshold; older day blocks moved to [CHANGELOG-2026-05a.md](CHANGELOG-2026-05a.md).

- **M3 closeout: parallel-lane bootstrap regression fixed and duplicate-event dedup added.** During M3-A/B/C parallel plugin landings, a bootstrap ordering regression appeared where preload calls in `src/main.ts` and plugin event registrations could race, producing duplicate event ids. Fixed by locking preload order before `registerAllPlugins(host)` and adding dedup guard in the events registry. Autoplay age 0->39 confirms no duplicate-id throw.

- **Milestone M5-A: DataPack contract and example pack complete**: Defined `DataPack` interface in `src/plugins/plugin_host.ts` with required fields `id`, `name`, optional `description` and `version`, plus optional sub-items: `events` (GameEvent[]), `choices` (WeeklyChoice[]), `activities` (Activity[]), and `lifecycleHooks` (discriminated union of AgeHook | PhaseStartHook | CareerEndHook). Implemented `DataPackRegistry` in `src/plugins/registries/data_pack_registry.ts` with routing logic: when a pack is registered, its sub-items are automatically routed into their respective registries (events -> event_registry, choices -> choice_registry, activities -> activity_registry, lifecycle hooks -> lifecycle_registry by discriminant). Created minimal example pack at `src/plugins/high_school/packs/example_pack.json` (1 event, 1 activity, 1 lifecycle hook). Created `src/plugins/high_school/packs/example_pack_loader.ts` following WP-M2-D-FIX loader pattern (fetch throws on !ok, preload awaits, sync getter throws if not preloaded). Updated `src/plugins/high_school/index.ts` to call `host.dataPacks.register(loadExamplePack())` in the plugin's `register()` method. Updated `tests/test_plugin_host.ts` with `testDataPackRouting()` verifying that pack registration routes sub-items to all sub-registries, and updated `testBootOrder()` and `testBootOrderNegativeNoHandlers()` to call `preloadExamplePack()`. Updated `tests/fixtures/fetch_mock.ts` with URL mapping for example_pack.json. No `as any`, all types strict; contract is architect-frozen after M5.

### Removals and Deprecations

- **M-B: deleted bespoke test runner `tests/run.ts`**: 81-line `spawnSync` orchestrator removed via `git rm`. Replaced by direct `node --import tsx --import ./tests/fixtures/csv_loader.mjs --test 'tests/test_*.ts'` invocation in the `test:node` npm script. The old runner enumerated 9 modules in a `TEST_MODULES` constant array; the new shape lets node:test discover every `tests/test_*.ts` file automatically and report by individual test name (56 named tests vs the previous 9 module-summary lines).
- **Audit cleanup: deleted dead preload/fetch loader exports, ESLint tooling, and the post-M5 fetch_mock stub**: applied audit-flagged dead-code removals (R1/R2/R5/R6). Deleted `tests/fixtures/fetch_mock.ts` (post-M5 no-op stub kept only for two test imports) along with its `setupFetchMock()` call sites in `tests/test_handler_registry.ts` and `tests/test_plugin_host.ts`. From all 9 plugin loaders (`src/plugins/childhood/{activities,events}_loader.ts`, `src/plugins/college/{activities,events}_loader.ts`, `src/plugins/high_school/{activities,events}_loader.ts`, `src/plugins/high_school/packs/example_pack_loader.ts`, `src/plugins/nfl/{activities,events}_loader.ts`) deleted the `preloadX()` no-op exports and the `fetchX()` Promise-wrapped exports; only the synchronous `loadX(): T[]` getter remains. Removed the matching 9 `preload*` imports and 9 `await preload*()` boot calls from `src/main.ts` (now `registerAllPlugins(host)` runs synchronously immediately after `buildPluginHost()`). Updated stale doc comments in the four plugin `index.ts` files (childhood, college, high_school, nfl) to reflect bundle-time JSON loading. Dropped `@playwright/test` from `package.json` devDependencies (never imported; only `playwright` is used by `tests/playwright/autoplay.mjs`). Deleted ESLint entirely: `git rm eslint.config.js`, removed `eslint` from devDependencies, removed the `"lint": "eslint ."` script and the dead `"main": "eslint.config.js"` field from `package.json`, removed the lint step + step name from `check_codebase.sh` (now 6 steps: typecheck, typecheck:lint, format:check, test:node, test:playwright, build) and updated the header comment + numbering. Total: 1 deleted fixture, 1 deleted ESLint config, 18 deleted exports (9 preload + 9 fetch) across 9 loaders, ~30 deleted lines in main.ts and the two test files. Bundle size: ~520KB before -> 507.3KB after (esbuild dead-code elim was already dropping the unused exports, so the on-disk shrink comes mainly from the doc comments). Verification: `npm run typecheck` exits 0; `npm run typecheck:lint` exits 0; `npm run test:node` reports `run.ts: all 9 test modules passed.` (handler_registry: 3/3 ok, test_plugin_host.ts: all tests passed); `bash check_codebase.sh --fast` summary `[PASS] typecheck / [PASS] typecheck:lint / [PASS] format:check / [PASS] test:node / [SKIP] test:playwright (--fast) / [SKIP] build (--fast) / PASS: 6 checks (0 failed).`; full `bash check_codebase.sh --skip-playwright` adds `[PASS] build` for `PASS: 6 checks (0 failed).`; `bash build_github_pages.sh` exits 0 with `dist/main.js` at 508K. `package-lock.json` regeneration via `npm install` is pending user approval (passthrough). Residual references to `@playwright/test` in `devel/setup_playwright.sh` (installs the runner; harmless), `docs/INSTALL.md`, `docs/PLAYWRIGHT_USAGE.md`, and the legacy CHANGELOG archive are left untouched per task boundaries (docs other than CHANGELOG out of scope) -- the autoplay harness uses bare `playwright` and does not need the `@playwright/test` test runner.

- **M2 aggregate removal summary**: `HS_ACTIVITIES` symbol fully removed from `src/activities.ts`; HS rows fully removed from `src/tabs.ts:PHASE_TABS`; `renderHSCareer` fully removed from `src/ui/career_widget.ts`; HS-only silent-fallback constants (`HS_ACTIVITIES_FALLBACK`, `HS_EVENTS_FALLBACK`) removed from the plugin loaders. No HS-specific switches remain in core.

### Decisions and Failures

- **M2 proof checkpoint PROCEEDED (reviewer agent verdict 2026-05-21)**: HS plugin shape is materially cleaner than pre-refactor (1 plugin tree vs 5 scattered files for adding an HS feature). M3 lanes (childhood, college, NFL) approved to start.
- **CareerContext frozen-after-M1 with one architect-approved exception**: `getPlayer(): Player` accessor (architect ruling 2026-05-20). `src/plugins/plugin_host.ts` carries the freeze guardrail comment above `PanelRegistration`. All future panels obtain the player via `ctx.getPlayer()`, never via a raw `player` field on `CareerContext`.
- **Deferred to post-M2 (parking-lot register at /tmp/post_m2_deferred.md)**: (1) seeded RNG baseline re-anchor (R2 HIGH audit finding; baseline currently informational only); (2) `lifecycle_registry` `sortHooks` thin-host decision (R3 MED); (3) `tests/test_plugin_host.ts` size justification for 862-line file (R1 LOW).
- **M3 closeout decision: phase folders (`src/childhood/`, `src/high_school/`, `src/college/`, `src/nfl_handlers/`) stay in tree; not moved into `src/plugins/<phase>/handlers/`.** Cost of moving ~95 lines outweighs benefit; plugins import and wire via `host.phases.register()`. Parallel-lane bootstrap regression discovered during M3-A/B/C landings and fixed via preload-order lock plus duplicate-event dedup guard (see Fixes).
- M4 (rules registry milestone) skipped per plan conditional gate: zero phase plugins consume host.rules.get(); simulator engine imports rules directly. Re-evaluate if a future plugin requires rule lookup.

- **Decided: HS weekly choices remain arc-phase-scoped and shared.** Choices JSON files (preseason, opening, midseason, stretch, postseason) are organized by game-week arc phase, not career phase, and are used across childhood, HS, college, and NFL. Moving them into the HS plugin would fragment a cross-phase resource. Left in `src/data/choices/`, no plugin-side loader created. Full choice pool lifecycle belongs to M3 deferred work if phase-specific filtering is needed.

- **WP-M6-A archive->src/plugins/scout_report relocation did not use git mv as required by docs/REPO_STYLE.md.** File history broken; cannot retroactively repair. Recorded for future audit (R1-BLOCKER-B2 audit finding 2026-05-21).

### Developer Tests and Notes

- **M3 closeout verification: autoplay age 0 to 39 green, all gates pass.** `bash build_github_pages.sh` exit 0; `npx tsc --noEmit -p tsconfig.lint.json` 0 errors; `npx tsx tests/test_plugin_host.ts` all tests passed; `npx tsx tests/check_plugin_boundaries.ts` no boundary violations; autoplay log `/tmp/autoplay_cleanup_verify.log` line 53 reaches age 39, no NEW PAGE ERROR, no duplicate-id throw.

- **Added fixture fetch mock for HS plugin loader tests**: created `tests/fixtures/fetch_mock.ts` that intercepts `globalThis.fetch` for Node.js test environment. Mock reads HS activities and events JSON from disk (`src/plugins/high_school/activities.json`, `src/plugins/high_school/events/high_school.json`) instead of making HTTP requests. Updated `tests/test_plugin_host.ts` and `tests/test_handler_registry.ts` to call `setupFetchMock()` at module load time. Both test files now call `preloadHsActivities()` and `preloadHsEvents()` during test setup without network errors. No fallback data in loaders; tests serve real JSON locally per WP-M2-D-FIX requirement. Verification: `npx tsx tests/test_plugin_host.ts` and `npx tsx tests/test_handler_registry.ts` both pass, `npx tsc --noEmit -p tsconfig.lint.json` zero errors, `bash check_codebase.sh` all tests pass.

## 2026-05-20

### Additions and New Features

- **High School plugin (M2-C): migrated activities, tabs, and career panel to PluginHost registration**: extracted HS activities array (`HS_ACTIVITIES`) from `src/activities.ts:63-104` into plugin-owned JSON (`src/plugins/high_school/activities.json`), loader (`src/plugins/high_school/activities_loader.ts`), and registry registration via `host.activities.registerMany(loadHsActivities())`. Extracted HS tabs (6 tab definitions for life, stats, activities, team, career, social) from `src/tabs.ts:27-34` into plugin-owned registration via `src/plugins/high_school/tabs.ts:registerHsTabs(host)` calling `host.ui.registerTab(...)` for each tab. Migrated HS career panel rendering (`renderHSCareer`) from `src/ui/career_widget.ts:122-148` into plugin-owned panel implementation at `src/plugins/high_school/panels/career_panel.ts`, registered via `host.ui.registerPanel(hsCareerPanel)` with frozen `render(ctx: GameContext): void` contract. Updated `src/plugins/high_school/index.ts` to call all three registrations in `register(host)`. Plugin integration points: `src/activities.ts` gains module-level `pluginHost` setter and uses `pluginHost.activities.getAll()` for HS phase; `src/tabs.ts` gains module-level `pluginHost` setter and uses `pluginHost.ui.getAllTabs()` filtered by HS phase for tab configuration; `src/ui/career_widget.ts` gains module-level `pluginHost` and `currentContext` setters to dispatch HS career rendering to registered panel. Boot order: `src/main.ts` calls `registerAllPlugins(host)` then `setPluginHost(host)`, `setTabsPluginHost(host)`, `setCareerPluginHost(host)` to wire dependencies before game starts; career context is set when game loop initializes. Verification: `bash check_codebase.sh` all tests green, `timeout 240 node tests/playwright/autoplay.mjs` reaches age 22 baseline with HS years (ages 14-17) rendering activities, tabs, and career panel identically to pre-refactor baseline. No `as any` casts, all types strictly inferred.

- **High School plugin (M2-A): extracted HS phase handlers into plugin registration layer**: migrated `hsFroshSophHandler` and `hsVarsityHandler` from core `src/core/register_handlers.ts` into new plugin-owned registration via `src/plugins/high_school/index.ts` and `src/plugins/high_school/phase_handler.ts`. Implemented GamePlugin contract: `highSchoolPlugin` exports name='high_school', version='0.1.0', register(host) calls phase handler registration via PluginHost facade. Plugin registration called from `src/plugins/register_plugins.ts:registerAllPlugins(host)` after core handler boot (childhood, college, NFL). Preserved zero business logic in plugin layer; registration is thin dispatch to existing HS handler implementations. Boot order: core handlers first (via registerAllHandlers), then plugins extend (via registerAllPlugins). Tests updated: `test_handler_registry.ts` now calls registerAllPlugins to register HS handlers; `test_plugin_host.ts` negative test updated to verify HS handlers register via plugin independent of core handlers. Boundary check passes; DOM-free registration layer confirmed. Smoke test baseline maintained: autoplay continues to age 22 without hang.

### Behavior or Interface Changes

- **WP-M2-A: high school phase handlers (`hsFroshSophHandler`, `hsVarsityHandler`) moved from `src/core/register_handlers.ts` to `src/plugins/high_school/phase_handler.ts`, registered via PluginHost.** Other phases (childhood, college, NFL) unchanged and continue to register through core. `register_handlers.ts` now registers 11 handlers instead of 13. Boot order: `registerAllHandlers()` runs first, then `registerAllPlugins(host)` runs; the HS phase registry entries appear only after both have completed.

### Fixes and Maintenance

- **Fixed autoplay PAGE ERROR at age 22: unfinished games causing season advancement blocker (WP-NFL-WEEK1)**: root cause was missing finalization step before weekly advancement. In `src/weekly/season_lifecycle.ts:advanceToNextWeek()`, added call to `simulateNonPlayerGames(activeEngine.season)` (line 83) before `advanceWeek()` to ensure all non-player games are recorded before the strict advancement check. Added import of `simulateNonPlayerGames` from `../season/season_simulator.js` (line 10). Defense-in-depth: also added same call in `src/weekly/game_handler.ts:simulateRestOfSeason()` loop (line 242) before `advanceWeek()` to prevent the same error during fast-forward. Error message "Cannot advance: 6 unfinished game(s) in week 1" no longer appears at age 22. Verification: `bash check_codebase.sh` all tests green, `timeout 240 node tests/playwright/autoplay.mjs` reaches final age 24 (progressed past former crash point at age 22), no PAGE ERROR.

- **Resolved GameContext type collision blocker (LANE A audit fix)**: fixed name collision between two distinct GameContext types in the codebase. Renamed `export interface GameContext` in `src/game_loop.ts` to `export interface GameLoopContext` (local shared-state interface for weekly game loop callbacks). Canonical `type GameContext = CareerContext` remains in `src/core/game_context.ts` (contract alias used by plugin lifecycle hooks in `src/plugins/plugin_host.ts`). Updated importers: `src/main.ts` now imports and uses `GameLoopContext` for game loop initialization. `src/game_loop.ts` exports remain unchanged semantically; pure type alias rename. Plugin hooks (`AgeHook`, `PhaseStartHook`, `CareerEndHook`) continue to reference canonical `GameContext` from core. Compilation clean: `npx tsc -p tsconfig.lint.json` zero errors, `bash check_codebase.sh` all tests green. One source of truth per type.
- **Removed Promise.race timeout wrapper from game initialization (LANE B audit fix 1)**: deleted Promise.race pattern with 30s timeout from `document.addEventListener('DOMContentLoaded')` handler in `src/main.ts` (lines 284-293). Issue: Promise.race does NOT cancel initGame(); if timeout fires, partial initialization persists without cleanup; setTimeout also leaks if initGame() resolves first (no clearTimeout). Pattern was a correctness hazard that masked hang instead of canceling it. New pattern: simple `.catch()` on initGame() promise with typed error handler, surfacing failures via error message in UI. Code: `document.addEventListener('DOMContentLoaded', () => { initGame().catch((error: unknown) => { ... }); });`. No timeout imposed; if hang occurs, catch will never fire and page remains in loading state (explicit failure better than silent timeout that doesn't cancel). Type checks: `error: unknown` parameter narrowed with `instanceof Error` check before accessing `.message`. Verification: `npx tsc -p tsconfig.lint.json` clean, `bash check_codebase.sh` all tests pass, DOM imports check 0 violations.
- **Replaced window.location.origin with absolute path in choice loader (LANE B audit fix 2)**: removed `window.location.origin` DOM access from `loadChoicePools()` in `src/weekly/choices.ts` (formerly line 56). Issue: window.location.origin is browser-global DOM access in non-UI data-loading module in sim-tree (src/weekly/); violates DOM-in-panels-only architectural rule; fragile in headless test environments. New pattern: fetch URLs use simple absolute path `/src/data/choices/${phase}.json` that resolves from page root without DOM globals. Kept parallel `Promise.all()` refactor: replaced serial for-loop with mapped async callbacks, all awaited together (improves load latency, fixes headless Chromium head-of-line blocking). DOM imports check: 0 violations across 9 sim-tree dirs after fix. Verification: `bash check_codebase.sh` passes, choice schemas load and validate (preseason 4, opening 4, midseason 4, stretch 4, postseason 3 choices), `npx tsc -p tsconfig.lint.json` clean.

### Decisions and Failures

- **Architect rejected `player: Player` raw field add to CareerContext during WP-M2-C cycle 2; approved `getPlayer(): Player` method accessor as the single M2 exception to the M1 freeze.** Root cause: CareerContext carries explicit "frozen after M1" comment (src/core/year_handler.ts line 3). Adding a raw field is contract mutation regardless of additive-vs-breaking semantics. Correct fix: method accessor is explicit about read-only intent, consistent with existing CareerContext method-only surface post-data-fields, and matches existing accessor pattern (updateStats, updateHeader already pass player in/out). Required changes: (1) removed `player: Player;` from CareerContext, added `getPlayer(): Player;` method declaration with comment "approved M2 exception to M1 freeze"; (2) in buildCareerContext (src/main.ts:87), replaced `player: currentPlayer!,` with `getPlayer: () => currentPlayer as Player`; (3) in hsCareerPanel.render (src/plugins/high_school/panels/career_panel.ts:18), replaced `ctx.player` with local const `player = ctx.getPlayer()`; (4) added guardrail comment in plugin_host.ts above PanelRegistration: "CareerContext (aliased as GameContext) surface is frozen after M1. Additions require architect approval. WP-M2-C precedent: getPlayer() accessor approved 2026-05." Rationale: harmless-field drift across M2-M7 would erase the freeze; method accessor is consistent with existing CareerContext method-only surface and explicit read-only intent.

- **Architect rejected unauthorized mutation of `PanelRegistration.render` signature during prior WP-M2-C-FIX cycle 1 attempt.** The frozen M1 contract is restored: `render(ctx: GameContext): void` with panels obtaining the player via `ctx.getPlayer()` accessor method and panels mounting DOM only inside their own `src/plugins/<plugin>/panels/*.ts` module. See [docs/PLUGIN_ARCHITECTURE.md](PLUGIN_ARCHITECTURE.md) "Panel render contract" for the binding rule.
